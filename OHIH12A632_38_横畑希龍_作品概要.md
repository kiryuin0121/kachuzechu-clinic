# 3校合同学内コンテスト 『滑舌クリニック』
## 成果物について

早口言葉を録音し、AIによる音声認識の結果をもとに「正確さ」「速さ」を採点するWebアプリケーションです。

## プロジェクトの概要

## プロジェクトの実行・閲覧方法

### 動作環境

- Python 3.13
- XAMPP（MySQL / MariaDB）
- OpenAI APIキー（音声認識に必要）
- SMTPサーバー（会員登録の確認メール送信に必要。Gmail等）
- MediaRecorder API に対応したブラウザ（Chrome / Edge 推奨）

### 手順

1. XAMPPのMySQLを起動し、phpMyAdmin等から `OH_IH12A632_38_横畑希龍_データベース.sql` を実行する
   - データベース `OH_PY23DB_IH12A_38` と4テーブルが作成され、お題70件が投入されます

2. 依存ライブラリをインストールする

   ```bash
   pip install -r requirements.txt
   ```

3. プロジェクト直下に `.env` を作成し、以下を設定する

   ```text
   SECRET_KEY=任意のランダム文字列

   OPENAI_API_KEY=OpenAIのAPIキー

   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=
   DB_NAME=OH_PY23DB_IH12A_38

   MAIL_SERVER=smtp.gmail.com
   MAIL_PORT=587
   MAIL_USE_TLS=true
   MAIL_USE_SSL=false
   MAIL_USERNAME=送信元メールアドレス
   MAIL_PASSWORD=アプリパスワード
   MAIL_DEFAULT_SENDER=送信元メールアドレス
   ```

4. アプリを起動する

   ```bash
   python app.py
   ```

5. ブラウザで `http://127.0.0.1:5000` を開く


### 使用ライブラリ

`requirements.txt` に記載しているライブラリと、本アプリでの用途は以下のとおりです。

| ライブラリ | 用途 |
| --- | --- |
| Flask | Webフレームワーク。ルーティング・テンプレート描画・セッションによるログイン管理に使用 |
| mysql-connector-python | MySQL / MariaDB への接続。ORMは使わず、SQLを直接実行する（`app/db.py`） |
| openai | OpenAI公式SDK。Whisper API（whisper-1）を呼び出して録音音声を文字起こしする（`app/services/speech.py`） |
| pykakasi | 漢字・カタカナをひらがなへ変換する。Whisperの認識結果をお題の読みと同じ表記に揃えて比較するために使用（`app/services/scoring.py`） |
| Flask-Mail | 会員登録・メールアドレス変更時の確認メール送信（`app/services/mail.py`） |
| python-dotenv | `.env` からAPIキー・DB接続情報・SMTP設定を読み込む（`app/__init__.py`） |
| pydantic[email] | zodのような感じでバリデーションルールのスキーマを定義して、フォームや採点APIの入力値検証のできるライブラリ。メールアドレス形式・パスワード長・録音時間の範囲・音声形式などをチェックする（`app/schemas.py`） |

提出フォルダの構成は以下のとおりです。

```text
OH_IH12A632_38_横畑希龍_プロジェクトフォルダ/
├── app.py                                  # 起動スクリプト
├── requirements.txt                        # 依存ライブラリ
└── app/
    ├── __init__.py       # create_app()（設定読込・Blueprint登録・エラー画面）
    ├── db.py             # DB接続の共通処理
    ├── schemas.py        # 入力値の検証（pydantic）
    ├── utils.py          # ログイン必須デコレーターなどの共通関数
    ├── routes/           # 画面・APIのルーティング
    │   ├── home.py       # トップ・難易度別お題一覧
    │   ├── practice.py   # 練習画面・採点API・録音再生
    │   ├── auth.py       # 会員登録・メール認証・ログイン・アカウント変更
    │   └── dashboard.py  # マイページ・設定画面
    ├── services/
    │   ├── speech.py     # Whisper APIによる音声認識・音声ファイル保存
    │   ├── scoring.py    # 採点ロジック
    │   └── mail.py       # Flask-Mailによるメール送信
    ├── templates/        # Jinja2テンプレート
    └── static/
        ├── js/recorder.js        # 録音・送信・結果表示
        └── uploads/audio/        # 会員の録音ファイル保存先
```

### 主な機能

**ゲスト（未ログイン）でも使える機能**

- 難易度1〜5から選んでお題（早口言葉70件）を一覧表示
- ブラウザ上で録音（MediaRecorder API）し、サーバーへ送信
- Whisper APIで音声認識し、以下の3つのスコアを表示
  - 正確さ：認識結果とお題の読み（ひらがな）の一致率
  - 速さ：目標時間に対する発話速度のズレ
  - 総合：正確さ7割・速さ3割で合算
- 認識結果と正解の差分を色分けして表示（一致 / 言い間違い / 言えていない / 余分）

**会員（ログイン済）で使える機能**

- メールアドレス・パスワードでの会員登録と、メールによる本人確認
- 練習結果の履歴保存（1回の練習につき1レコード）
- お題ごとの自己ベスト・前回スコアの表示
- お題ごとの最新録音の再生
- パスワード変更・メールアドレス変更（変更先アドレスへの確認メール付き）

### 練習の流れ

1. トップページで難易度を選ぶ
2. お題カードを選んで練習画面へ
3. 「録音開始」を押し、お題を3回連続で読み上げて「録音停止」
4. 音声認識・採点が行われ、スコアと差分が表示される
5. 会員の場合は履歴に保存され、自己ベスト・前回スコアが更新される

## 工夫点

### 1. セキュリティへの配慮

- 録音ファイルの取得は専用ルート（`/practice/audio/<お題ID>`）経由に限定し、ログイン中のユーザー自身のファイルだけを返す。`/static/uploads/audio/` への直接アクセスは403で遮断
- パスワードは `werkzeug.security` でハッシュ化して保存
- メール認証トークンは `secrets` で生成し、30分の有効期限付き。使用後は削除
- SQLはすべてプレースホルダーを使用し、文字列連結で組み立てない
- 採点APIに送られる値（お題ID・録音時間・音声形式・サイズ）は pydantic でサーバー側でも検証し、改ざんされた値や異常値を弾く
- アップロードサイズは16MBに制限

### 2. 録音ファイルは「ユーザー×お題」ごとに最新1件だけ保持

会員の録音は `audio_user_{user_id}_twister_{お題ID}.webm` という固定ファイル名で保存し、同じお題を再度録音したら上書きします。
履歴レコードは練習のたびに増えますが、音声ファイルは1ユーザーあたり最大70件に収まり、ストレージが際限なく増えることを防いでいます。

### 3. ゲストでも気軽に試せる設計

会員登録なしでも練習から採点まで一連の流れが使えるようにし、まず試してもらってから会員登録の価値（履歴・自己ベスト・録音再生）を感じてもらえる導線にしています。
ゲストの音声は一時ファイルとして処理し、採点の成否にかかわらず `finally` で必ず削除するため、サーバーに残りません。

### 4. 責務を分けたシンプルな構成

ルーティング（routes）・外部サービス連携（services）・入力検証（schemas）・DB接続（db）などの責務に応じてファイル分割を行っています。
